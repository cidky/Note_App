<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nandaadisaputra.noteapp">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoteApp"
        tools:targetApi="31">

        <!-- MainActivity sudah terdaftar dengan benar -->
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tambahkan deklarasi AddNoteActivity -->
        <activity
            android:name=".ui.activity.AddNoteActivity"
            android:exported="true" />

        <!-- Tambahkan deklarasi DetailActivity -->
        <activity
            android:name=".ui.activity.DetailActivity"
            android:exported="true" />

        <!-- Tambahkan deklarasi untuk EditActivity -->
        <activity
            android:name=".ui.activity.EditNoteActivity"
            android:exported="true" />
    </application>

</manifest>
